{
  "name": "red-mushroom-staff-bot",
  "version": "1.0.0",
  "description": "One configurable Discord bot for applications, staff movements, announcements, and audit logs.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "check": "node --check src/index.js && node --check src/commands.js && node --check src/database.js && node --check src/extra-commands.js && node --check src/extra-features.js && node --check src/community-features.js && node --check src/staff-movements.js",
    "test": "node --test",
    "verify:mongodb": "node scripts/verify-mongodb.js"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "dependencies": {
    "discord.js": "^14.27.0",
    "dotenv": "^17.2.2",
    "mongodb": "^7.6.0"
  }
}
