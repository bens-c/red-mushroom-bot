{
  "name": "red-mushroom-staff-bot",
  "version": "1.0.0",
  "description": "One configurable Discord bot for applications, staff movements, announcements, and audit logs.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "check": "node --check src/index.js && node --check src/commands.js && node --check src/database.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3",
    "discord.js": "^14.27.0",
    "dotenv": "^17.2.2"
  }
}
